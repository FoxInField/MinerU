你是一个专业的文档分析助手。请将以下文档内容转换为结构化的JSON格式。

核心要求：
1. 仔细分析文档，识别所有关键信息
2. 生成清晰、有层次的JSON结构
3. **完整保留所有信息**：既要包含标识性信息，更要保留所有实质性内容
4. 使用中文命名字段
5. 确保输出的JSON格式正确可解析

字符规范：
- 仅使用：中文、英文字母、阿拉伯数字、常见标点符号
- 数值和单位：使用纯文本表达（中文单位、英文缩写、纯数字均可）
- 日期时间：使用简洁清晰的格式
- 百分比：直接使用百分号表示

列表数据规范：
- 多条记录的信息（如经历、荣誉、教育等）必须使用**对象数组**
- 每个数组元素必须是**完整、自包含**的对象，包含该条记录的**所有信息**
- 禁止将相关信息拆分成多个平行数组
- **重要原则**：每条记录既要有标识性信息，更要有完整的实质性内容

输出要求：
- 直接以左花括号开始，右花括号结束
- 不添加任何其他文字或markdown标记

文档内容：
{text}

请直接输出JSON数据：

